# Docs

Step-by-step guides for `ush` live here.

Available today:

- [`typed-errors.md`](./typed-errors.md): how `.ush` typed errors, Zig-style `Problem!T` signatures, `raise`, inferred `# raises:`, and Rust-like `?` propagation fit together

For runnable scripts, see [`../examples/README.md`](../examples/README.md).
